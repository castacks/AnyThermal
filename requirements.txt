--extra-index-url https://download.pytorch.org/whl/cu121

# setuptools>=70 removes pkg_resources.packaging, which torch 2.1.2's
# torch.utils.cpp_extension (imported by mmcv) still relies on. Keep it <70.
setuptools<70

torch==2.1.2
torchvision==0.16.2
torchaudio==2.1.2
numpy<2
tyro
matplotlib
tqdm
faiss-gpu
pyyaml
natsort
scipy
opencv-python
scikit-learn
imageio
pyproj
h5py
pandas
pacmap
seaborn
pytorch-lightning
pytorch-metric-learning
wandb
pytorchvideo
gdown
ftfy
transformers==4.36.2
timm
einops
easydict
mmcv==1.7.2
# Runtime deps of pytorch3d (needed by the depth baseline). pytorch3d itself is
# NOT listed here because it builds from source against the installed torch and
# therefore must be installed AFTER torch with build isolation disabled:
#   pip install --no-build-isolation git+https://github.com/facebookresearch/pytorch3d.git
# See the "Monocular Thermal Depth Estimation" section of the README.
fvcore
iopath
ninja
path
tensorboard
pycryptodomex
python-gnupg
rospkg
