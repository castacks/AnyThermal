losses:
  attention_alignment:
    weight: 1.0
    report_only: true
    params:
      mode: "kl"

  # patch_nce:
  #   weight: 1.0
  #   report_only: true
  #   params:
  #     temperature: 0.07
  #     patch_radius: 1
  #     max_patches: 1024

  global_contrastive:
    weight: 1.0
    report_only: false
    params:
      temperature: 0.07

  global_cosine:
    weight: 1.0
    report_only: true
    params:
      return_mean: true
  
  patch_nce_single_positive:
    weight: 1.0
    report_only: true
    params:
      temperature: 0.07
      patch_radius: 1
      exclude_anchor_from_denominator: false
